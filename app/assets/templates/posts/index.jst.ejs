<div id="posts">
  <% for (var i = 0, len = posts.models.length; i < len; i++) { %>

    <div class="post" id="post-<%= posts.models[i].get('id') %>">
      <%= this.show_tmpl({post: posts.models[i]}) %>
    </div> <!-- .post -->

  <% } %>
</div> <!-- #posts -->
